doctype html
html(lang="es")
  head
    meta(charset="utf-8")
    meta(name="viewport", content="width=device-width, initial-scale=1")
    meta(name="description", content=info.infoHeader)
    meta(name="author", content="Antorrg. Antonio Ricardo Rodriguez Gramajo")
    meta(name="generator", content="Hugo 0.104.2")
    title Proyecto #{info.title}
    link(href="/app-styles.css" rel="stylesheet")
    link(rel='icon' href='/images/nomades2.png')
  body
    main
      section.py-5.text-center.container
        .row.py-lg-5
          .col-lg-6.col-md-8.mx-auto
            h1.fw-light Proyecto #{info.title}
            img.card-img-top(src=info.landing, alt='Card image' style='35%')
            p.lead.text-muted.mt-2=info.infoBody
            p
              a.btn.btn-secondary.my-2(href="../") Volver
    .album.py-5.bg-light
      .container
        - const chunk = (arr, size) => {
        -   return arr.reduce((acc, _, i) => {
        -     if (i % size) {
        -       acc[acc.length - 1].push(arr[i]);
        -     } else {
        -       acc.push([arr[i]]);
        -     }
        -     return acc;
        -   }, []);
        - };
        - const rows = chunk(items, 3);
        each row in rows
          .row.row-cols-1.row-cols-sm-2.row-cols-md-3.g-3
            each item in row
              .col
                .card.shadow-sm
                  img.card-img-top(src=item.img, alt='Card image' style='30%')
                  .card-body
                    p.card-text= item.text
                    .d-flex.justify-content-between.align-items-center
                      .btn-group
                        a.btn.btn-sm.btn-outline-secondary(href=`/detalle/item/${item.id}`) Ver mas
    footer.text-muted.py-5
      .container
        p.float-end.mb-1
          a.btn.btn-primary(href="#") Volver arriba
        p.mb-1 Album example is &copy; Bootstrap, but please download and customize it for yourself!
        p.mb-0
          | New to Bootstrap? 
          a(href="/") Visit the homepage
  //-script(type="module" src='/js/bootstrap.bundle.min.js')
  script(type="module" src='/main.js')

